from setuptools import setup,find_packages

setup(name='BCC_HTCS',
      version='0.1.0',
      packages=find_packages(),
      description='This is a packsge for breakthrough curve calculation',
      author='Eden Stuart(Yizhen Situ) & Qiao')
